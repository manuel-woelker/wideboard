### 18:22 - Replace project name references with wideboard [gpt-5-codex]

**User Prompt:**
Replace legacy project-name references with wideboard across all files.

**Issues Encountered:**
- No `docs/journal` directory existed, so it was created before logging the change.

**Decisions Made:**
- Replaced explicit legacy project-name references with `wideboard` in repository documentation files.
- Kept all other wording intact to avoid unintended content changes.

**Technical Info Consulted:**
- Repository-wide case-insensitive text scans.
- Matching files: `README.md`, `CONTRIBUTING.md`, `AGENTS.md`

**Assumptions Made:**
- The request targets literal legacy naming references only.

**Other Notes:**
- Added this journal entry as required by `AGENTS.md`.

### 19:15 - Scaffold baseline React web UI with Vite, Vitest, TypeScript, and Emotion [gpt-5-codex]

**User Prompt:**
In the ui folder create a basic vite, vitest, typescript, react, emotion, web ui

**Issues Encountered:**
- The initial `check` script used nested `pnpm` calls, which failed under `tool-tool` execution.
- TypeScript initially missed Vitest globals (`describe`, `it`, `expect`) during typecheck.

**Decisions Made:**
- Built a minimal Vite + React + TypeScript scaffold directly in `ui/`.
- Added Emotion-based styling in `src/App.tsx` to provide a basic web UI.
- Added a colocated Vitest + Testing Library test file (`src/App.test.tsx`) for baseline coverage.
- Updated `check` to run direct commands (`tsc`, `vitest`, `vite build`) for reliable CI-like execution.

**Technical Info Consulted:**
- Existing repository layout and `AGENTS.md` command requirements.
- Existing journal entry format in `docs/journal/2026-02-17.md`.

**Assumptions Made:**
- A clean baseline scaffold was desired in `ui/` because no source project files existed there.
- Using `gpt-5-codex` is the correct model identifier for this session.

**Other Notes:**
- Ran `../tool-tool.exe pnpm run format` and `../tool-tool.exe pnpm run check`; both complete successfully.

### 19:25 - Add GitHub Actions workflow for GitHub Pages deployment [gpt-5-codex]

**User Prompt:**
create a github action to deploy to github pages

**Issues Encountered:**
- None.

**Decisions Made:**
- Added a new workflow at `.github/workflows/deploy-pages.yml` that triggers on pushes to `main` and manual dispatch.
- Built the UI from `ui/` using `tool-tool` and uploaded `ui/dist` as the Pages artifact.
- Set the Vite base path to `/${{ github.event.repository.name }}/` so repository-hosted Pages deployments resolve assets correctly.

**Technical Info Consulted:**
- `AGENTS.md` command requirements (`tool-tool`/`tool-tool.exe` usage).
- `ui/package.json` build scripts and `ui/vite.config.ts`.

**Assumptions Made:**
- The default deployment target branch for this repository is `main`.
- The Pages site is served as a repository Pages site rather than a user/org root site.

**Other Notes:**
- Workflow uses official Pages actions: `configure-pages`, `upload-pages-artifact`, and `deploy-pages`.

### 19:45 - Fix Pages workflow trigger branch mismatch [gpt-5-codex]

**User Prompt:**
the page is not deployed, github does not see any actions

**Issues Encountered:**
- Workflow trigger was configured for `main` only while the repository is on `master`.

**Decisions Made:**
- Updated `.github/workflows/deploy-pages.yml` to trigger on pushes to both `master` and `main`.

**Technical Info Consulted:**
- Local git branch (`master`) and current workflow file trigger configuration.

**Assumptions Made:**
- GitHub default branch is currently `master`, so enabling both branch names is the safest immediate fix.

**Other Notes:**
- `workflow_dispatch` remains enabled for manual runs from the Actions tab.
