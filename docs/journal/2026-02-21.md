### 15:43 - Allow right-click context menu unless right-button drag pan occurs [gpt-5-codex]

**User Prompt:**
when right clicking, the context menu should still open, only when dragging should the context menu be suppressed

**Issues Encountered:**
- Existing behavior prevented right-click context menu unconditionally via both `pointerdown` and `contextmenu` handling.

**Decisions Made:**
- Added drag detection for right-button pan and only set context-menu suppression after movement reaches a small threshold.
- Kept middle-button panning prevention behavior intact.
- Added separate tests for right-click without drag (allowed) and right-drag pan (suppressed).

**Technical Info Consulted:**
- `ui/src/board/BoardComponent.tsx`
- `ui/src/board/BoardComponent.test.tsx`
- Project verification commands: `../tool-tool.exe pnpm run format`, `../tool-tool.exe pnpm run check`

**Assumptions Made:**
- "Dragging" means pointer movement while right button pan is active, not a simple press/release right-click.

**Other Notes:**
- `pnpm run check` passed; existing React `act(...)` warnings in tests remain and were not introduced by this change.
